import styled from 'styled-components'

import {
  border,
  color,
  layout,
  opacity,
  space,
  typography,
  compose
} from 'styled-system'

const StyledP = styled.p`
  ${color}
  ${space}
  ${layout}
  ${typography}
    ${border}




`

export default StyledP
