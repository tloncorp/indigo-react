import styled from 'styled-components';

import Text from './Text';

const T0 = styled(Text)``;

T0.defaultProps = {
  lineHeight: 'short',
  fontSize: 0,
  margin: 0,
  fontWeight: 400,
};

export default T0;
