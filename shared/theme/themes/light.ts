import colors from '../colors';
import core from '../core';

const theme = {
  colors: {
    ...colors,
    primary: colors.blue1,
    util: '#FFFF00',
    none: 'rgba(0,0,0,0)',
  },
  ...core,
};

export default theme;
